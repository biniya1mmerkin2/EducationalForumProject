import React from "react";
import { Container, Typography } from "@mui/material";

const Catagories = () => {
  return (
    <Container maxWidth="md">
      <Typography variant="h4">Catagories Page</Typography>
    </Container>
  );
};

export default Catagories;
